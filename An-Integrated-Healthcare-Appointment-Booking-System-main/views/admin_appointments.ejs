<%- include('partials/header', { activePage: 'admin-appointments' }) %>

<h2 class="section-title mb-3">All Appointments</h2>

<div class="d-flex justify-content-between align-items-center mb-2">
  <p class="small text-muted mb-0">
    Filter and manage all appointments. Use export to download CSV.
  </p>
  <a href="/admin/appointments/export?<%= new URLSearchParams(filters).toString() %>"
     class="btn btn-sm btn-outline-success rounded-pill">
    <i class="bi bi-file-earmark-spreadsheet me-1"></i> Export CSV
  </a>
</div>

<form method="GET" class="row g-2 g-md-3 align-items-end mb-3">
  <div class="col-md-4">
    <label class="form-label small">Search</label>
    <input
      type="text"
      name="q"
      value="<%= (filters && filters.q) || '' %>"
      class="form-control form-control-sm rounded-3"
      placeholder="Patient, doctor, email..."
    />
  </div>
  <div class="col-md-2">
    <label class="form-label small">Status</label>
    <select name="status" class="form-select form-select-sm rounded-3">
      <option value="">All</option>
      <option value="pending" <%= filters && filters.status==='pending'?'selected':'' %>>Pending</option>
      <option value="confirmed" <%= filters && filters.status==='confirmed'?'selected':'' %>>Confirmed</option>
      <option value="cancelled" <%= filters && filters.status==='cancelled'?'selected':'' %>>Cancelled</option>
      <option value="completed" <%= filters && filters.status==='completed'?'selected':'' %>>Completed</option>
    </select>
  </div>
  <div class="col-md-2">
    <label class="form-label small">From</label>
    <input
      type="date"
      name="date_from"
      value="<%= (filters && filters.date_from) || '' %>"
      class="form-control form-control-sm rounded-3"
    />
  </div>
  <div class="col-md-2">
    <label class="form-label small">To</label>
    <input
      type="date"
      name="date_to"
      value="<%= (filters && filters.date_to) || '' %>"
      class="form-control form-control-sm rounded-3"
    />
  </div>
  <div class="col-md-2 d-grid">
    <button type="submit" class="btn btn-outline-primary btn-sm rounded-pill">
      Filter
    </button>
  </div>
</form>

<div class="card border-0 shadow-sm">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table table-hover app-table mb-0">
        <thead>
          <tr>
            <th>Patient</th>
            <th>Email</th>
            <th>Doctor</th>
            <th>Specialty</th>
            <th>Date</th>
            <th>Time</th>
            <th>Status</th>
            <th class="text-end">Change Status</th>
          </tr>
        </thead>
        <tbody>
          <% if (!appointments.length) { %>
          <tr>
            <td colspan="8" class="text-center py-4 text-muted">
              No appointments found.
            </td>
          </tr>
          <% } %>

          <% appointments.forEach(function(a) { %>
          <tr>
            <td class="small">
              <strong><%= a.user_name %></strong>
            </td>
            <td class="small text-muted"><%= a.user_email %></td>
            <td class="small"><%= a.doctor_name %></td>
            <td class="small text-muted"><%= a.specialty_name %></td>
            <td class="small"><%= formatDate(a.appointment_date) %></td>
            <td class="small"><%= formatTime(a.appointment_time) %></td>
            <td class="small">
              <span class="status-badge status-badge-<%= a.status %>">
                <%= a.status %>
              </span>
            </td>
            <td class="text-end">
              <a href="/admin/appointments/<%= a.id %>/detail"
                class="btn btn-sm btn-outline-secondary rounded-pill me-1">
                Details
              </a>

              <form class="d-inline-flex align-items-center gap-2"
                    method="POST" action="/admin/appointments/<%= a.id %>/status">
                <select name="status" class="form-select form-select-sm rounded-pill">
                  <option value="pending"   <%= a.status === 'pending'   ? 'selected' : '' %>>pending</option>
                  <option value="confirmed" <%= a.status === 'confirmed' ? 'selected' : '' %>>confirmed</option>
                  <option value="cancelled" <%= a.status === 'cancelled' ? 'selected' : '' %>>cancelled</option>
                  <option value="completed" <%= a.status === 'completed' ? 'selected' : '' %>>completed</option>
                </select>
                <button type="submit" class="btn btn-sm btn-outline-primary rounded-pill">
                  Update
                </button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<%- include('partials/footer') %>